#
# Configuration template for the .../pppoe/node.tag/idle-timeout parameter
#
# This parameter sets the time in seconds after which an idle
# connection will be closed. Disconnection of idle connections will not
# be performed if this parameter is not set, or if the value of this
# parameter is set to 0.
#

type: u32

help: "Amount of time in seconds to wait before disconnecting an idle session.  Never disconnect if set to zero"

syntax: ($(@) >= 0) ; "Idle timeout can not be negative"

update: "sudo sed -i '/^idle/d' /etc/ppp/peers/pppoe$(../@) ; \
        sudo echo idle $(@) >> /etc/ppp/peers/pppoe$(../@)"

delete: "sudo sed -i '/^idle/d' /etc/ppp/peers/pppoe$(../@)"
