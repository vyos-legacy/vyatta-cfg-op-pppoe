#
# Configuration template for the .../pppoe/node.tag/mtu parameter
#
# Set the IP layer Maximum Transmission Unit (MTU) for the PPPOE interface.
#

type: u32

help: "Set the IP layer Maximum Transmission Unit (MTU), in bytes"

syntax: (($(@) > 68) && ($(@) <= 1492)) ; "MTU must be between 68 and 1492"

update: "sudo sed -i '/^mtu/d' /etc/ppp/peers/pppoe$(../@) ; \
        sudo echo mtu $(@) >> /etc/ppp/peers/pppoe$(../@)"

delete: "sudo sed -i '/^mtu/d' /etc/ppp/peers/pppoe$(../@) ; \
        sudo echo mtu 1492 >> /etc/ppp/peers/pppoe$(../@)"



